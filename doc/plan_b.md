# Plan B′：可微碰撞代价驱动的抓取位姿生成（简化版）

> **项目决策记录**（v1.3）：取消动力学级判定，判定规则降级为**运动学级刚体碰撞判定**（EEF 与目标物体的可微碰撞代价）；训练与推理共用同一可微代价函数。碰撞代价实现参考 DiffPhyRobot 的"可微查询点 + detached 表面点"梯度绕行模式。

**版本**：v1.3（判定降级为运动学碰撞；全可微单一代价链路；Genesis 退出训练环）

---

## 1. 定位与与 Plan A 的关系

Plan B′ 是与 Plan A（stage0 → stage1 → stage2 解析管线）并行互补的**学习式方案**。本项目不使用教师模型蒸馏——训练信号完全来自**可微碰撞代价**（§4），无任何位姿标签：

| 角色 | 承担方 | 说明 |
|---|---|---|
| 判定器（训练 + 推理统一） | 可微碰撞代价函数（§4） | 运动学级 EEF–物体刚体碰撞；训练时为损失，推理时阈值化为成败判定 |
| 快速推理前端 | 本方案的条件生成网络 | 单次前向替代 stage0 搜索 + stage1 解析计算 |
| 冷启动锚点 | 标称系 $T_{nom}(\hat{a})$ + 零初始化输出头 | 网络初始输出 = 解析标称位姿（§3.1） |
| 可选离线抽检（不在管道内） | Plan A 的 stage2 | 动力学级验收工具，仅离线评估用，不进入训练回路 |

**已明示的取舍**：判定降级后，滑落 / 重心不稳 / 动态扰动**失去直接检测能力**，仅剩几何代理缓解（§4.4 摩擦锥判据 + 对称接触 + 接触窗口）。此为本方案的核心边界，记录于 §9。

### 1.1 对原始 Plan B 的四处修正

| 原方案 | 问题 | 修正 |
|---|---|---|
| (1) 单峰回归输出 | 多模态平均陷阱：两个好抓取的平均是坏抓取 | **多假设生成头**（条件扩散 / MDN） |
| (2) 可微运动学模型用神经网络 | 学已知的解析关系：近似误差毒化梯度、训练成本、不可解释 | **可微解析层**（stage1 公式 PyTorch 化，零训练、精确） |
| (3) 动力学 loss 端到端回传 | 接触力读数与离散成败不可微 | **判定降级（v1.3）**：运动学碰撞代价全可微，单一链路端到端 |
| (4) teacher 蒸馏 warm-start | 项目决策不采用 | 零初始化输出头 + SDF 引导课程（§5.1） |

---

## 2. 总体架构

```
                     ┌──────────────────────────────────────────┐
                     │               推理路径                     │
                     └──────────────────────────────────────────┘
 EEF 几何 G_EEF + 物体点云/几何 + 近进方向 â
        │
   [几何编码器] ──▶ 条件 c
        │
   [条件扩散头] × K 次采样 ──▶ ξ₁..ₖ ──(exp map)──▶ T_TCP 假设
        │
   [可微解析运动学层] ──▶ T_task + 手指采样点集
        │
   [可微碰撞代价]（§4）── 阈值化 ──▶ 成败判定 + 排序
        │
   首个通过者执行；全败 → 回传失败原因，请求更换 â（stage0 §8 协议）

                     ┌──────────────────────────────────────────┐
                     │              训练（离线）路径（全可微）      │
                     └──────────────────────────────────────────┘
 T1: 碰撞代价从零训练（§5.1，零初始化 + SDF 引导课程）
        └──▶ T2: 全约束端到端精调（§5.2，接触窗口 + 对向 + 走廊）
                  └──▶ T3: 开度联合精调 + 失败回流（§5.3）
 【无仿真在环】整条链路为 torch/quadrants 前向计算图，Genesis 不参与训练
```

核心数据流约定：

- 复用 stage1 的**切空间参数化**（$\boldsymbol{\xi} = (\Delta t, \Delta R) \in \mathbb{R}^6$，平移在前）；
- 所有计算在规划参考系 {W} 下统一；物体几何先规范到 {O} 系，代价计算时变换回 {W}；
- 输出多假设按代价升序交付，Top-K 语义与 stage0 一致。

---

## 3. 网络设计

### 3.1 输出参数化与冷启动

- **标称系 $T_{nom}(\hat{a})$**：由 â + 截面基解析构造（物体几何中心投影 + 名义深度），不依赖任何学习组件；
- **网络输出**：相对 $T_{nom}$ 的切空间扰动 $\boldsymbol{\xi} \in \mathbb{R}^6$（exp map 到 SE(3)，与 stage1 扰动模型同构）；各分量物理语义：沿 â 深度、垂直 â 平面内偏移 ×2、Roll、Pitch/Tilt 微调；
- **零初始化输出头**：扩散头 $x_0$-prediction 最后一层零初始化——训练开始时 $\hat{x}_0 = 0$，采样输出恒为 $T_{nom}(\hat{a})$（解析级粗对齐质量），碰撞代价从第一步起即有梯度；
- **生成头**：条件扩散头（DDPM/DDIM，$\boldsymbol{\xi}$ 为噪声空间）；备选 MDN（消融）。K 次采样天然覆盖多模态。

### 3.2 几何编码

- 物体：点云 encoder（PointNet++ 或轻量 attention）或 基元参数直接嵌入；输出 256 维条件向量；
- EEF：参数化几何（$G_{EEF}$：指厚/指长/开度范围/掌部）固定嵌入；
- 噪声增广与 stage0 §7 感知误差模型一致。

---

## 4. 可微碰撞代价（本方案核心）

**判定规则**：EEF 与目标物体的刚体碰撞状态——具体为**指垫受控接触 + 非接触面防撞**的结构化代价。实现参考 DiffPhyRobot 的梯度绕行模式（env_robot_2d.py `find_vec_to_obstacles`）。

### 4.1 梯度链路（DiffPhyRobot 模式映射）

| DiffPhyRobot（避障） | 本方案（抓取） | 可微性 |
|---|---|---|
| 机器人检测点（步态剖面 `_gait_profile_positions`，torch 可微） | **EEF 指垫采样点** $p_{f,j} = \mathrm{FK}(T_{task}, q_{open})$——解析运动学直出，可微 | ✓ 梯度流经 $T_{task}$ 回网络 |
| 障碍物表面最近点（quadrants kernel `find_nearest_pt_2d_topk`，detached） | **物体表面最近点 / SDF 查询**（基元闭式 SDF 或 mesh kernel oracle） | ✗ 视为局部常数（一阶近似） |
| 碰撞向量 `npt − p` → 避障惩罚 | 采样点–表面距离 → **接触窗口** / **防撞惩罚**（语义反转） | ✓ |

**基元物体**（盒/圆柱/球）：闭式 SDF（圆柱 `‖p−c‖−r`、盒分象限，同 DiffPhyRobot `_clearance_to_obstacles` L69-97 的形式），挂上 autograd 即全可微，**无需 kernel**。
**Mesh 物体**：预计算 SDF 场（三线性插值，处处可微）或 quadrants 最近点 oracle（表面点 detach，梯度只走查询点）。

### 4.2 代价项

设指垫内侧采样点集 $\{p_{f,j}^{L}\}, \{p_{f,j}^{R}\}$（每指 $n_p$ 个），非接触面（指背/指侧/掌部）采样点集 $\{p_{b,k}\}$，物体 SDF 为 $D_O(\cdot)$（负值 = 内部）：

| 项 | 公式 | 语义 |
|---|---|---|
| **接触窗口** | $\mathcal{L}_{touch} = \overline{\mathrm{relu}(D_O(p_{f,j}) - \delta_{hi}) + \mathrm{relu}(\delta_{lo} - D_O(p_{f,j}))}$ | 指垫穿透落在 $[\delta_{lo}, \delta_{hi}]$（如 [0, 2mm]）为零罚；过浅无接触、过深是硬撞 |
| **双侧对齐** | $\mathcal{L}_{2s} = \mathrm{relu}(D_{min}^{L} - \delta_{hi}) + \mathrm{relu}(D_{min}^{R} - \delta_{hi})$ | 双指都必须有接触（$D_{min}$ = 该指所有采样点最小距离） |
| **对称接触** | $\mathcal{L}_{sym} = \overline{\|D_O(p_{f,j}^{L}) - D_O(p_{f,j}^{R})\|}$ | 两指穿透深度均衡（力矩平衡的几何代理） |
| **走廊防撞** | $\mathcal{L}_{cor} = \overline{\mathrm{relu}(\delta_{clear} - D_O(p_{b,k}))}$ | 非接触面与物体保持间隙——与 DiffPhyRobot 避障代价同构 |
| **开度合法** | $\mathcal{L}_{w} = \mathrm{relu}(q_{open} - q_{max}) + \mathrm{relu}(q_{min} - q_{open})$ | 手指开度在工作行程内 |

**推理期判定（阈值化，非训练损失）**：

$$\text{success} = \left( D_{min}^{L}, D_{min}^{R} \in [\delta_{lo}, \delta_{hi}] \right) \wedge \left( \theta_{opp} \le \theta_{max} \right) \wedge \left( \min_k D_O(p_{b,k}) \ge \delta_{clear} \right)$$

$\theta_{opp}$ = 两指接触点连线与指面法向的夹角（对向性）。

### 4.3 接近过程建模（准静态推进）

抓取是过程不是单帧：从 $T_{task}$ 沿 $-\hat{a}$ 回退 $\ell_{retreat}$ 的**扫掠检查**——指垫采样点沿接近路径积分采样（$n_\ell$ 个中间构型），走廊代价取路径最大值。纯几何积分，可微。

### 4.4 动力学 gap 的几何缓解（先验，非验证）

| 失去的能力 | 几何代理 |
|---|---|
| 摩擦保持（滑落） | 摩擦锥几何判据：对向法向夹角 $\le \pi - 2\arctan(\mu)$（µ 为先验，保守取 0.5） |
| 力闭合 | 对向双侧接触 + 对称穿透（$\mathcal{L}_{sym}$）+ 接触点高度差约束 |
| 提升稳定（重心） | 接触区域中心与物体质心投影对齐项（质心由几何估计） |

这些是**先验假设而非检测**——若实际 µ 更低或物体内部质量分布异常，本方案无法发现（§9 风险 R1）。

---

## 5. 训练流程（全可微，无仿真在环）

### 5.1 阶段 T1：碰撞代价从零训练

- **初始化**：零初始化输出头（初始网络输出 = $T_{nom}(\hat{a})$）；
- **课程**：
  - **C0（SDF 引导）**：$\mathcal{L}_{guide} = \overline{D_O(p_{f,j})}$——有符号距离本身（非 hinge），远场处处有梯度，把假设吸向物体表面；
  - **C1（粗对齐）**：$\mathcal{L}_{guide} + \mathcal{L}_{cor} + \mathcal{L}_{2s}$；
- **损失**：$\mathcal{L}_{DSM} + \lambda_{geo} \cdot \mathcal{L}_{C0/C1}$；
- **多模态**：扩散框架对"碰撞代价低的位姿集合"建模，多模态由代价地形隐式给出。

### 5.2 阶段 T2：全约束端到端精调

- **损失**：$\mathcal{L}_{DSM} + \lambda_{geo} \cdot (\mathcal{L}_{touch} + \mathcal{L}_{2s} + \mathcal{L}_{sym} + \mathcal{L}_{cor} + \mathcal{L}_{w})$（权重见 §12）；
- **作用**：接触窗口精调 + 扫掠走廊检查（§4.3）全部进入计算图。

### 5.3 阶段 T3：开度联合精调 + 失败回流

- **开度 $q_{open}$ 联合优化**：作为可微变量与位姿联合训练（初始闭式：$q_{open} \approx w_{obj}/2 + \delta_{hi}$，物体宽度 $w_{obj}$ 由几何估计）——替代 stage2 的手指力精调；
- **失败回流（DAGGER）**：推理期判定失败的四元组 `(输入条件, 位姿假设, 失败原因, 诊断量)` 回流训练集——失败原因含 `no_contact`（窗口过浅）/ `hard_collision`（穿透过深）/ `corridor_violation`（防撞失败）/ `not_antipodal`（对向角超限），以对比项进入回归目标；
- **可选离线抽检**：每隔 N 个 epoch 用 stage2（Genesis 前向）对 held-out 样本做动力学抽检，**仅作监控指标**（如滑落率），不产生训练梯度——保留动力学 gap 的可观测性而不增加在线成本。

---

## 6. 数据管线（纯几何自监督，无标注无仿真）

| 环节 | 设计 |
|---|---|
| 物体来源 | 基元参数扫描（长方体/圆柱/球）+ 网格库子集（如 ABC），总量 $\sim 10^4$ |
| 方向采样 | 每物体 $\sim 10$ 个 $\hat{a}$（半空间均匀 + 偏置） |
| 训练信号 | 碰撞代价自监督（§4）——**无需任何位姿标签、无需仿真打分** |
| 增广 | {O} 系规范姿态对齐、对称类标签（解析）、点云扰动（与 stage0 §7 一致） |
| 成本 | 每步 = 网络前向 + 解析运动学 + SDF 查询——GPU 批量，吞吐高于仿真在环一个量级 |

---

## 7. 接口

### 7.1 输出（Top-K 假设）

| 字段 | 类型 | 说明 |
|---|---|---|
| `T_task_k` | SE(3) | 任务目标位姿（{W} 系），按碰撞代价升序 |
| `q_open_k` | float | 手指开度建议 |
| `cost_k` | float | 碰撞代价总值（判定与排序共用） |
| `contact_state_k` | dict | 双侧 $D_{min}$、对向角、走廊最小间隙（诊断量） |

### 7.2 失败回传

与 stage0.md §8 协议一致：候选按代价升序下发 → 上游逐个尝试 → 失败原因分类回传（候选性 / 方向性）→ 方向性失败请求更换 â。

---

## 8. 引擎约束（Genesis 1.3.3 实测，历史记录）

v1.3 判定降级后 **Genesis 退出训练与推理回路**，以下实测结论保留为历史记录（stage2/Plan A 仍依赖）：

| 路线 | 实测结论 |
|---|---|
| `set_pos`/`set_quat` 实体级位姿注入 | 有梯度通道（AD/FD = 1.000；sceneless gs.Tensor 且 1-D 形状） |
| PD 位置目标可微化 | 显式抛异常；可微控制入口仅 `control_dofs_force` |
| 接触力读数 | detached（get_contacts / 力传感器） |
| 可微查询面 | 仅 `scene.get_state()` 族 |

本方案不再受这些约束影响（纯几何计算图）；碰撞代价实现如需最近点 kernel，使用 quadrants（与 stage1_demo.py 的 QdBackend 同栈）。

---

## 9. 风险与缓解

| 风险 | 缓解 |
|---|---|
| **R1 动力学 gap 无验证器兜底**（滑落/重心/扰动不可检，判定降级的直接代价） | 摩擦锥几何判据（µ 保守先验）+ 对称接触 + 质心对齐（§4.4）；T3 可选 stage2 离线抽检监控滑落率；上线前用 stage2 做一次全量离线验收 |
| R2 碰撞代价的局部极小（贴面不接触 / 单侧接触） | C0 SDF 引导 + 多假设 K 采样 + 失败回流对比项 |
| R3 多模态平均 | 扩散头多假设；代价地形隐式多模态 |
| R4 冷启动零梯度 | 零初始化（初始 = 标称位姿）+ SDF 引导远场梯度 |
| R5 Mesh 物体的 SDF 预计算成本 | 基元场景闭式 SDF 零成本；mesh 离线预计算一次复用；oracle kernel 兜底 |
| R6 开度–位姿耦合的非凸性 | 开度闭式初始化（物体宽度先验）+ 联合微调 |

---

## 10. 验收

| 指标 | 定义 | 目标 |
|---|---|---|
| Top-K 通过率 | K 个假设中 ≥1 个通过运动学判定 | ≥ 90%（基元集）；held-out ≥ 80% |
| Top-1 通过率 | 首假设通过率 | ≥ Top-K 的 70% |
| 推理延迟 | 网络前向 + 解析层 + 代价计算（GPU） | ≤ 50 ms |
| 泛化衰减 | held-out 物体集相对训练集的下降 | ≤ 10 个百分点 |
| 冷启动基线 | 零初始化网络（= 标称位姿）的判定通过率 | > 0（验证 $T_{nom}$ 质量） |
| **动力学抽检（离线监控，非验收线）** | stage2 前向验证的运动学通过样本滑落率 | 记录并趋势下降；无硬阈值（R1 取舍的量化观测） |
| 代价–梯度保真 | 代价函数对位姿的 AD/FD 交叉验证 | ≥ 0.99（纯解析链路应接近精确） |

---

## 11. 方法选型总结

| 设计点 | 选择 | 放弃方案及其失败模式 |
|---|---|---|
| 判定规则 | 运动学可微碰撞代价（§4） | 动力学级判定（需 Genesis 在环，工程重、吞吐低——已按项目决策降级） |
| 碰撞代价实现 | 基元闭式 SDF + mesh SDF/oracle（DiffPhyRobot 梯度绕行模式） | mesh-mesh 精确碰撞（不可微或代价过高） |
| 输出头 | 条件扩散多假设 | 单峰回归（多模态平均陷阱） |
| 运动学 | 可微解析层 | 神经网络学习（近似误差毒化梯度） |
| 训练信号 | 碰撞代价自监督（无标签无仿真） | teacher 蒸馏（已决策不采用）；仿真打分回归（依赖 Genesis，v1.3 已移除） |
| 冷启动 | 零初始化 + SDF 引导 | 纯 hinge 从零（远场零梯度） |
| 开度 | 闭式初始化 + 联合微调 | 固定开度（对宽度变化不鲁棒）；力控仿真精调（依赖 Genesis） |

---

## 12. 参数表（待标定）

| 参数 | 符号 | 初始值 | 说明 |
|---|---|---|---|
| 假设数 | $K$ | 8 | 推理期采样数 |
| 扩散步数 | — | 10 | DDIM 确定性采样 |
| 网络规模 | — | ~5M 参数 | encoder 256 维 + 扩散头 |
| 指垫采样点数 | $n_p$ | 每指 16 | 指垫内侧网格 |
| 接触窗口 | $[\delta_{lo}, \delta_{hi}]$ | [0, 2mm] | 允许穿透带 |
| 防撞间隙 | $\delta_{clear}$ | 5mm | 非接触面走廊 |
| 摩擦系数先验 | $\mu$ | 0.5 | 摩擦锥判据（保守） |
| 对向角阈值 | $\theta_{max}$ | 15° | 与 stage0 θ_fric 同源 |
| 扫掠中间构型数 | $n_\ell$ | 8 | 接近路径采样（§4.3） |
| 代价项权重 | $\lambda_{touch}/\lambda_{2s}/\lambda_{sym}/\lambda_{cor}/\lambda_{w}$ | 1.0 / 1.0 / 0.5 / 0.5 / 0.3 | T2 |
| SDF 引导权重 | $\lambda_{guide}$ | 0.5 | C0/C1 |
| 几何代价总权重 | $\lambda_{geo}$ | 0.3 | 相对 $\mathcal{L}_{DSM}$ |

---

## 13. 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-23 | 初稿：解析层替代运动学网络、采样–打分–回归、多假设生成头 |
| v1.1 | 2026-09-23 | 移除 teacher 蒸馏：零初始化 + SDF 引导从零训练；数据管线仿真自生成 |
| v1.2 | 2026-09-23 | 实测确认 set_pos/set_quat 位姿梯度通道；T3 升级双通道（可微精调 + 采样–打分–回归） |
| v1.3 | 2026-09-24 | **判定降级（项目决策）**：运动学级可微碰撞判定替代动力学级；碰撞代价参考 DiffPhyRobot 梯度绕行模式（可微查询点 + detached 表面点）；Genesis 退出训练/推理回路；T3 改为开度联合精调 + 失败回流；新增动力学 gap 几何缓解与离线抽检监控 |
